{
  "name": "jarvis",
  "version": "0.1.0",
  "private": true,
  "description": "JARVIS personal assistant (see docs/jarvis for the specification)",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b packages/shared packages/core",
    "test": "node --import tsx --test --test-reporter=dot \"packages/*/test/**/*.test.ts\"",
    "schemas": "tsx packages/shared/scripts/export-schemas.ts",
    "review": "tsc -b packages/shared packages/core && tsx packages/shared/scripts/export-schemas.ts --check && tsx tools/spec-conformance.ts && npm test"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "tsx": "^4.23.15",
    "typescript": "^5.9.3"
  }
}
