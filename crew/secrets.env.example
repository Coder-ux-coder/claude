# Your API keys and other secrets for the projects the team builds.
# Copy to ~/.crew/secrets.env (crew setup does this). This file is never committed,
# and every value is blanked out of the chat, logs and reports.
#
# EXAMPLE_API_KEY=paste-your-key-here
# STRIPE_SECRET_KEY=
# OPENWEATHER_API_KEY=
